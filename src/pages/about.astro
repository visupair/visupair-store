---
import Layout from "../layouts/Layout.astro";
import Button from "../components/Button.astro";
export const prerender = true;
---

<Layout
  title="About Us — Visupair"
  description="We're two artists selling our own work — fashion, illustrations, 3D models, and more. No middlemen, just art."
>
  <Fragment slot="head">
    <script is:inline>
      (function () {
        try {
          var t = window.visupairGetTheme
            ? window.visupairGetTheme()
            : "dark";
          function preload(href) {
            var l = document.createElement("link");
            l.rel = "preload";
            l.as = "image";
            l.href = href;
            document.head.appendChild(l);
          }
          preload("/images/hero/Gul-" + t + ".svg");
          preload("/images/hero/Vali-" + t + ".svg");
        } catch (e) {}
      })();
    </script>
  </Fragment>

  <div class="about-wrapper">
    <div class="about-island">
      <!-- Lamp Lottie — top center -->
      <div class="about-lamp" id="about-lamp-container" aria-hidden="true">
      </div>

      <!-- Three-column layout -->
      <div class="about-grid">
        <!-- Left: Gul character -->
        <div class="about-char about-char--left">
          <img
            id="about-gul"
            class="about-char__img"
            src="/images/hero/Gul-dark.svg"
            alt="Gul — artist and co-founder of Visupair"
            width="1352"
            height="1761"
            loading="eager"
            fetchpriority="high"
            decoding="async"
            draggable="false"
            ondragstart="return false;"
          />
        </div>

        <!-- Center: text content -->
        <div class="about-content">
          <div class="about-badge animate-text above-fold-reveal" style="--anim-delay: 0s;">
            About Us
          </div>

          <h1 class="about-title animate-text above-fold-reveal" style="--anim-delay: 0.1s;">
            Who We Are
          </h1>

          <p class="about-subtitle animate-text above-fold-reveal" style="--anim-delay: 0.2s;">
            We're Gul and Vali — two artists making things we actually care
            about. Fashion, illustrations, digital art, 3D models. We sell it
            all ourselves, directly to you.
          </p>

          <div class="about-actions animate-text above-fold-reveal" style="--anim-delay: 0.3s;">
            <Button href="/portfolio" variant="primary" label="See Our Work" />
            <Button
              href="/contact"
              variant="secondary"
              label="Contact Us"
              icon={false}
            />
          </div>
        </div>

        <!-- Right: Vali character -->
        <div class="about-char about-char--right">
          <img
            id="about-vali"
            class="about-char__img"
            src="/images/hero/Vali-dark.svg"
            alt="Vali — artist and co-founder of Visupair"
            width="1352"
            height="1815"
            loading="eager"
            fetchpriority="high"
            decoding="async"
            draggable="false"
            ondragstart="return false;"
          />
        </div>
        <script is:inline>
          (function () {
            try {
              var t = window.visupairGetTheme
                ? window.visupairGetTheme()
                : "dark";
              var gul = document.getElementById("about-gul");
              var vali = document.getElementById("about-vali");
              if (gul) gul.src = "/images/hero/Gul-" + t + ".svg";
              if (vali) vali.src = "/images/hero/Vali-" + t + ".svg";
            } catch (e) {}
          })();
        </script>
      </div>
    </div>
  </div>

  <!-- Story section: image (left, TBD) + narrative (right) -->
  <div class="about-dual-wrapper">
    <div class="about-dual-layout">
      <div class="about-dual-panel about-dual-panel--media">
        <div class="about-dual-media-frame" data-about-media-spotlight>
          <img
            class="about-dual-media-img"
            src="/images/hero/aboutus.webp"
            alt="Gul and Vali — Visupair co-founders"
            width="1200"
            height="800"
            loading="lazy"
            decoding="async"
          />
          <div class="about-dual-media-overlay" aria-hidden="true"></div>
        </div>
      </div>

      <article
        class="about-dual-panel about-dual-panel--story"
        aria-labelledby="about-story-heading"
      >
        <h2
          class="about-story-heading animate-text"
          id="about-story-heading"
          style="--anim-delay: 0s;"
        >
          This space didn't start as a business idea.
          <br />
          <span class="about-story-heading__accent"
            >It started as a life built around art.</span
          >
        </h2>

        <div
          class="about-story-body animate-text"
          style="--anim-delay: 0.08s;"
        >
          <p>
            For over a decade, painting has been woven into our everyday rhythm
            &mdash; canvases, oils, and long quiet hours of learning to truly
            see. How light shifts. How texture holds a feeling. How color can
            say something words can't quite reach. That foundation shaped
            everything that came after.
          </p>
          <p>
            Curiosity kept pulling us forward. Six years ago we stepped into
            digital creation and found a whole new language &mdash; one built
            from visual design, technology, and the thrill of making something
            that didn't exist before. Then fashion arrived. Not as a pivot, but
            as a natural next step. For us, clothing has never felt separate
            from art. It's just another surface where shapes, ideas, and
            stories can take form &mdash; and be worn.
          </p>
          <p>
            <strong>Visupair</strong> is where all of it comes together.
          </p>
          <p>
            We design in 3D, build digital patterns, and bring them to life as
            real garments and objects. But those same pieces can also live
            entirely in the digital world &mdash; as 3D models, wearable
            avatars, digital collectibles &mdash; because we're equally
            fascinated by how fashion and art exist across both realities. We
            call this <em>phygital</em> creation: the freedom to move an idea
            from screen to material, and back again, without losing any of its
            meaning.
          </p>
          <p>
            We create our own pieces &mdash; garments, accessories, paintings,
            digital works. Some of them are available to find a home with you,
            in whichever form feels right.
          </p>
        </div>

        <div
          class="about-story-actions animate-text"
          style="--anim-delay: 0.16s;"
        >
          <Button href="/store" variant="primary" label="Visit Store" />
          <Button
            href="/contact"
            variant="secondary"
            label="Contact Us"
            icon={false}
          />
        </div>
      </article>
    </div>
  </div>

  <!-- Services & collaboration -->
  <div class="about-dual-wrapper">
    <div class="about-dual-layout about-dual-layout--reverse-desktop">
      <div class="about-dual-panel about-dual-panel--media">
        <div class="about-dual-media-frame" data-about-media-spotlight>
          <img
            class="about-dual-media-img"
            src="/images/hero/aboutservices.webp"
            alt="Visupair creative services — collaboration with artists and brands"
            width="1200"
            height="800"
            loading="lazy"
            decoding="async"
          />
          <div class="about-dual-media-overlay" aria-hidden="true"></div>
        </div>
      </div>

      <article
        class="about-dual-panel about-dual-panel--story"
        aria-labelledby="about-services-heading"
      >
        <h2
          class="about-story-heading animate-text"
          id="about-services-heading"
          style="--anim-delay: 0s;"
        >
          We also work with others &mdash; and this side of what we do comes
          straight from who we are.
        </h2>

        <div
          class="about-story-body animate-text"
          style="--anim-delay: 0.08s;"
        >
          <p>
            We collaborate with artists, independent designers, and brands to
            build the visual presence their work deserves. This means logos,
            websites, color palettes, creative direction, lookbooks, and full
            visual identities &mdash; everything that shapes how the world first
            experiences what you do.
          </p>
          <p>
            But what makes us different isn't the list of services. It's where
            we come from.
          </p>
          <p>
            We know what it feels like to be the artist on the other side
            &mdash; to have work that matters deeply and struggle to show it in
            a way that feels true. That experience sits at the heart of every
            project we take on. We don't just design for you. We think with you,
            understand your creative language, and build something that actually
            feels like yours.
          </p>
          <p>
            For artists and independent designers, we understand the particular
            challenge of presenting creative work without it feeling
            over-produced or disconnected from who you are. Whether you need a
            portfolio, a brand identity, or a complete digital home for your
            practice &mdash; we build it in a way that stays honest to your
            vision.
          </p>
          <p>
            For brands, we bring an artistic sensibility that goes beyond
            aesthetics. We think about meaning, consistency, and the kind of
            visual language that builds genuine recognition over time.
          </p>
        </div>

        <div
          class="about-story-actions about-story-actions--stacked animate-text"
          style="--anim-delay: 0.16s;"
        >
          <Button href="/services" variant="primary" label="Explore Services" />
          <Button
            href="/contact"
            variant="secondary"
            label="Contact Us"
            icon={false}
          />
        </div>
      </article>
    </div>
  </div>

  <!-- Workshops & courses: image left + story (same layout as first dual section) -->
  <div class="about-dual-wrapper">
    <div class="about-dual-layout">
      <div class="about-dual-panel about-dual-panel--media">
        <div class="about-dual-media-frame" data-about-media-spotlight>
          <img
            class="about-dual-media-img"
            src="/images/hero/aboutcourses.webp"
            alt="Visupair workshops and courses — oil painting, digital design, and creative sessions"
            width="1200"
            height="800"
            loading="lazy"
            decoding="async"
          />
          <div class="about-dual-media-overlay" aria-hidden="true"></div>
        </div>
      </div>

      <article
        class="about-dual-panel about-dual-panel--story"
        aria-labelledby="about-courses-heading"
      >
        <h2
          class="about-story-heading animate-text"
          id="about-courses-heading"
          style="--anim-delay: 0s;"
        >
          We love sharing what we know — and we believe that
          <br />
          <span class="about-story-heading__accent"
            >opening up a creative process is just as valuable as the finished work.</span
          >
        </h2>

        <div
          class="about-story-body animate-text"
          style="--anim-delay: 0.08s;"
        >
          <p>
            We organise workshops and courses in oil painting, digital design,
            and 3D &amp; phygital creation &mdash; as well as art nights, where
            creativity happens in a relaxed, open atmosphere. Whether you're a
            complete beginner or simply curious, there's a place for you here.
          </p>
          <p>
            If you'd like to join us or find out what's coming up, we'd love to
            hear from you.
          </p>
        </div>

        <div
          class="about-story-actions animate-text"
          style="--anim-delay: 0.16s;"
        >
          <Button href="/courses" variant="primary" label="Explore Courses" />
          <Button
            href="/contact"
            variant="secondary"
            label="Contact Us"
            icon={false}
          />
        </div>
      </article>
    </div>
  </div>

  <!-- Theme-aware SVG + Lottie switching -->
  <script>
    type Theme = "dark" | "light";

    let lottie: any = null;
    let lampAnim: any = null;
    let currentTheme: Theme | null = null;
    let themeObserver: MutationObserver | null = null;
    let intersectionObserver: IntersectionObserver | null = null;
    let hasCompletedAboutClientNav = false;
    let aboutSpotlightCleanup: (() => void) | null = null;

    const getTheme = (): Theme =>
      document.documentElement.hasAttribute("data-theme") ? "dark" : "light";

    const swapSVGs = (theme: Theme) => {
      const gul = document.getElementById(
        "about-gul",
      ) as HTMLImageElement | null;
      const vali = document.getElementById(
        "about-vali",
      ) as HTMLImageElement | null;
      if (gul) gul.src = `/images/hero/Gul-${theme}.svg`;
      if (vali) vali.src = `/images/hero/Vali-${theme}.svg`;
    };

    const loadLamp = async (theme: Theme) => {
      const container = document.getElementById("about-lamp-container");
      if (!container) return;

      // Destroy animation if theme changed
      if (lampAnim && theme !== currentTheme) {
        lampAnim.destroy();
        lampAnim = null;
        container.innerHTML = "";
        container.classList.remove("lamp-loaded");
      }

      // Already loaded for this theme — just show it
      const hasAnimationInDOM = container.querySelector("svg") !== null;
      if (currentTheme === theme && lampAnim && hasAnimationInDOM) {
        container.classList.add("lamp-loaded");
        return;
      }

      try {
        // Preserve the lottie library across page transitions —
        // only the animation instance is destroyed, not the library
        if (!lottie) {
          const module = await import("lottie-web/build/player/lottie_light");
          lottie = module.default || module;
        }

        container.innerHTML = "";
        lampAnim = lottie.loadAnimation({
          container,
          renderer: "svg",
          autoplay: true,
          loop: true,
          path: `/images/hero/lamp-${theme}.json`,
        });

        // Fade in smoothly only after SVG is ready in DOM
        lampAnim.addEventListener("DOMLoaded", () => {
          container.classList.add("lamp-loaded");
        });

        currentTheme = theme;
      } catch (e) {
        console.error("Failed to load Lottie lamp:", e);
      }
    };

    const applyTheme = (theme: Theme) => {
      swapSVGs(theme);
      loadLamp(theme);
    };

    const onThemeChange = () => {
      const theme = getTheme();
      swapSVGs(theme);
      void loadLamp(theme);
    };

    // Only disconnect observers when user navigates away.
    // The animation KEEPS PLAYING during the transition — same as index.astro.
    const cleanupObservers = () => {
      aboutSpotlightCleanup?.();
      aboutSpotlightCleanup = null;
      themeObserver?.disconnect();
      themeObserver = null;
      intersectionObserver?.disconnect();
      intersectionObserver = null;
    };

    const initAboutMediaSpotlight = () => {
      aboutSpotlightCleanup?.();
      const frames = document.querySelectorAll<HTMLElement>(
        "[data-about-media-spotlight]",
      );
      if (!frames.length) return;
      if (window.matchMedia("(prefers-reduced-motion: reduce)").matches) return;

      const mqMobile = window.matchMedia("(max-width: 900px)");
      const isMobileLayout = () => mqMobile.matches;

      const GROW_MS = 4200;
      const GROW_MS_MOBILE = 3200;
      const TOP_BOOST = 0.55;
      const TOP_BOOST_MOBILE = 0.35;

      const disposers: (() => void)[] = [];

      frames.forEach((frame) => {
        let active = false;
        let t0 = 0;
        let py = 0;
        let raf = 0;
        let scrollYAtInteract = 0;

        const reset = () => {
          active = false;
          cancelAnimationFrame(raf);
          frame.style.removeProperty("--spotlight-r");
          frame.classList.remove("is-spotlight-active");
        };

        const syncSpot = (cx: number, cy: number) => {
          const r = frame.getBoundingClientRect();
          py = cy;
          frame.style.setProperty("--spot-x", `${Math.round(cx - r.left)}px`);
          frame.style.setProperty("--spot-y", `${Math.round(cy - r.top)}px`);
        };

        const tick = () => {
          if (!active) return;
          const r = frame.getBoundingClientRect();
          const w = r.width;
          const h = r.height;
          const mobile = isMobileLayout();
          const growMs = mobile ? GROW_MS_MOBILE : GROW_MS;
          const topBoost = mobile ? TOP_BOOST_MOBILE : TOP_BOOST;
          const elapsed = performance.now() - t0;
          const time = Math.min(1, elapsed / growMs);
          const yN =
            h > 0 ? Math.max(0, Math.min(1, (py - r.top) / h)) : 0.5;
          const boost = (1 - yN) * topBoost;
          const mix = Math.min(1, time + boost * time);
          const tEase = 1 - (1 - mix) * (1 - mix);
          const minR = Math.max(64, Math.min(w, h) * (mobile ? 0.14 : 0.11));
          const maxR = Math.max(w, h) * (mobile ? 0.98 : 0.92);
          const radiusPx = minR + (maxR - minR) * tEase;
          frame.style.setProperty("--spotlight-r", `${Math.round(radiusPx)}px`);
          if (mix < 0.999) raf = requestAnimationFrame(tick);
        };

        const begin = () => {
          active = true;
          t0 = performance.now();
          scrollYAtInteract = window.scrollY;
          frame.classList.add("is-spotlight-active");
          cancelAnimationFrame(raf);
          const r = frame.getBoundingClientRect();
          const w = r.width;
          const h = r.height;
          const mobile = isMobileLayout();
          const minR = Math.max(64, Math.min(w, h) * (mobile ? 0.14 : 0.11));
          frame.style.setProperty("--spotlight-r", `${Math.round(minR)}px`);
          tick();
        };

        const onEnter = () => {
          if (isMobileLayout()) return;
          const r = frame.getBoundingClientRect();
          syncSpot(r.left + r.width / 2, r.top + r.height / 2);
          begin();
        };

        const onMove = (e: MouseEvent) => {
          if (isMobileLayout()) return;
          syncSpot(e.clientX, e.clientY);
        };

        const onTouchStart = (e: TouchEvent) => {
          const touch = e.touches[0];
          if (!touch) return;
          syncSpot(touch.clientX, touch.clientY);
          if (isMobileLayout()) begin();
          else if (!active) begin();
        };

        const onTouchMove = (e: TouchEvent) => {
          const touch = e.touches[0];
          if (!touch) return;
          syncSpot(touch.clientX, touch.clientY);
        };

        const onTouchEnd = () => {
          if (!isMobileLayout()) reset();
        };

        const onLeave = () => {
          if (isMobileLayout()) return;
          reset();
        };

        const onWindowScroll = () => {
          if (!isMobileLayout() || !active) return;
          if (window.scrollY > scrollYAtInteract + 28) reset();
        };

        frame.addEventListener("mouseenter", onEnter);
        frame.addEventListener("mousemove", onMove);
        frame.addEventListener("mouseleave", onLeave);
        frame.addEventListener("touchstart", onTouchStart, { passive: true });
        frame.addEventListener("touchmove", onTouchMove, { passive: true });
        frame.addEventListener("touchend", onTouchEnd);
        frame.addEventListener("touchcancel", onTouchEnd);
        window.addEventListener("scroll", onWindowScroll, { passive: true });

        disposers.push(() => {
          window.removeEventListener("scroll", onWindowScroll);
          reset();
          frame.removeEventListener("mouseenter", onEnter);
          frame.removeEventListener("mousemove", onMove);
          frame.removeEventListener("mouseleave", onLeave);
          frame.removeEventListener("touchstart", onTouchStart);
          frame.removeEventListener("touchmove", onTouchMove);
          frame.removeEventListener("touchend", onTouchEnd);
          frame.removeEventListener("touchcancel", onTouchEnd);
        });
      });

      aboutSpotlightCleanup = () => {
        disposers.forEach((d) => d());
        disposers.length = 0;
      };
    };

    // Called after the old DOM is fully replaced — safe to destroy animation now.
    const cleanupAnimation = () => {
      if (lampAnim) {
        lampAnim.destroy();
        lampAnim = null;
      }
      currentTheme = null;
    };

    const restartAboveFoldReveal = (selector: string) => {
      if (window.matchMedia("(prefers-reduced-motion: reduce)").matches) {
        return;
      }
      document.querySelectorAll(selector).forEach((el) => {
        const h = el as HTMLElement;
        h.style.animation = "none";
        void h.offsetHeight;
        h.style.removeProperty("animation");
      });
    };

    const init = () => {
      // Full reset before re-initialising on new page visit
      cleanupObservers();
      cleanupAnimation();
      applyTheme(getTheme());

      themeObserver = new MutationObserver(onThemeChange);
      themeObserver.observe(document.documentElement, {
        attributes: true,
        attributeFilter: ["data-theme"],
      });

      // Text animation observer
      const textObserver = new IntersectionObserver(
        (entries) => {
          entries.forEach((entry) => {
            if (entry.isIntersecting) {
              entry.target.classList.add("is-visible");
              textObserver.unobserve(entry.target);
            }
          });
        },
        { rootMargin: "0px 0px -50px 0px", threshold: 0.1 },
      );

      document.querySelectorAll(".animate-text").forEach((el) => {
        if (
          el.classList.contains("is-visible") ||
          el.classList.contains("above-fold-reveal")
        ) {
          return;
        }
        textObserver.observe(el);
      });

      const container = document.getElementById("about-lamp-container");
      if (container) {
        intersectionObserver = new IntersectionObserver(
          (entries) => {
            if (entries[0].isIntersecting) {
              if (lampAnim) lampAnim.play();
            } else {
              if (lampAnim) lampAnim.pause();
            }
          },
          { threshold: 0.7 },
        );
        intersectionObserver.observe(container);
      }

      initAboutMediaSpotlight();

      if (hasCompletedAboutClientNav) {
        requestAnimationFrame(() => {
          requestAnimationFrame(() => {
            restartAboveFoldReveal(
              ".about-content .animate-text.above-fold-reveal",
            );
          });
        });
      }
      hasCompletedAboutClientNav = true;
    };

    // Before transition starts: stop observing, but let animation finish visually
    document.addEventListener("astro:before-preparation", cleanupObservers);
    // After DOM swap: destroy old animation instance
    document.addEventListener("astro:after-swap", cleanupAnimation);
    // New page ready: boot everything up fresh
    document.addEventListener("astro:page-load", init);
  </script>

  <style>
    /* ── Reuse same blob keyframes as Home page ── */
    @keyframes blobDrift1 {
      0% {
        transform: translate(0%, 0%) scale(1);
      }
      20% {
        transform: translate(-12%, 18%) scale(1.08);
      }
      45% {
        transform: translate(8%, -20%) scale(1.04);
      }
      70% {
        transform: translate(-18%, -8%) scale(1.12);
      }
      100% {
        transform: translate(0%, 0%) scale(1);
      }
    }

    @keyframes blobDrift2 {
      0% {
        transform: translate(0%, 0%) scale(1);
      }
      25% {
        transform: translate(15%, -15%) scale(1.06);
      }
      55% {
        transform: translate(-10%, 20%) scale(1.1);
      }
      80% {
        transform: translate(12%, 10%) scale(1.04);
      }
      100% {
        transform: translate(0%, 0%) scale(1);
      }
    }

    /* ═══════════════════════════════════════
       TEXT ANIMATIONS (Slide Up & Fade In)
       ═══════════════════════════════════════ */
    .animate-text {
      opacity: 0;
      transform: translateY(20px);
      transition:
        opacity 0.8s cubic-bezier(0.16, 1, 0.3, 1),
        transform 0.8s cubic-bezier(0.16, 1, 0.3, 1);
      transition-delay: var(--anim-delay, 0s);
      will-change: opacity, transform;
    }

    .animate-text.is-visible {
      opacity: 1;
      transform: translateY(0);
    }

    .about-content .animate-text.above-fold-reveal {
      opacity: 0;
      transform: translateY(20px);
      transition: none;
      animation: aboutTextReveal 0.72s cubic-bezier(0.16, 1, 0.3, 1) forwards;
      animation-delay: var(--anim-delay, 0s);
    }

    @keyframes aboutTextReveal {
      from {
        opacity: 0;
        transform: translateY(20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    @media (prefers-reduced-motion: reduce) {
      .about-content .animate-text.above-fold-reveal {
        animation: none;
        opacity: 1;
        transform: none;
      }
    }

    @keyframes islandFadeIn {
      from {
        opacity: 0;
        transform: translateY(12px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    /* ── Island wrapper ── */
    .about-wrapper {
      width: 100%;
      padding: 0 12px;
      box-sizing: border-box;
    }

    .about-island {
      width: 100%;
      border-radius: var(--radius-xl);
      border: 1px solid var(--border-subtle);
      overflow: hidden;
      position: relative;
      background: var(--surface);
      box-shadow:
        inset 0 0 100px rgba(0, 0, 0, 0.02),
        var(--shadow-island);
      transition: box-shadow 0.4s ease;
      animation: islandFadeIn 0.6s cubic-bezier(0.2, 0, 0, 1) both;
    }

    :global([data-theme="dark"]) .about-island {
      /* background: linear-gradient(135deg, #1a1635, #2a2442); */
      box-shadow:
        inset 0 0 100px rgba(0, 0, 0, 0.2),
        var(--shadow-island);
    }

    /* Glow effects - shared styles */
    .about-island::before,
    .about-island::after {
      content: "";
      position: absolute;
      pointer-events: none;
      z-index: 0;
      border-radius: 50%;
      will-change: transform;
    }

    /* ── Blob base ── */
    .about-island::before {
      top: -60%;
      right: -40%;
      width: clamp(800px, 150vw, 2400px);
      aspect-ratio: 1;
      background: transparent;
    }

    .about-island::after {
      bottom: -50%;
      left: -35%;
      width: clamp(700px, 130vw, 2000px);
      aspect-ratio: 1;
      background: transparent;
    }

    :global([data-theme="dark"]) .about-island::before {
      background: radial-gradient(
        closest-side,
        rgba(163, 142, 255, 0.4) 0%,
        rgba(163, 142, 255, 0.15) 35%,
        rgba(163, 142, 255, 0.02) 70%,
        rgba(163, 142, 255, 0) 100%
      );
      opacity: 0.5;
      animation: blobDrift1 22s infinite alternate ease-in-out;
    }

    :global([data-theme="dark"]) .about-island::after {
      background: radial-gradient(
        closest-side,
        rgba(132, 128, 255, 0.35) 0%,
        rgba(132, 128, 255, 0.12) 35%,
        rgba(132, 128, 255, 0.02) 70%,
        rgba(132, 128, 255, 0) 100%
      );
      opacity: 0.5;
      animation: blobDrift2 28s infinite alternate ease-in-out;
    }

    /* ── Lamp — top center (Absolute Overlay) ── */
    .about-lamp {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      z-index: 3; /* above blobs (0), chars (1) and content (1) */
      display: flex;
      justify-content: center;
      pointer-events: none;
      height: clamp(350px, 44vw, 640px);
      margin: 0;
      /* Start hidden, fade in after animation SVG is ready */
      opacity: 0;
      transition: opacity 0.4s ease;
    }

    .about-lamp.lamp-loaded {
      opacity: 1;
    }

    .about-lamp > * {
      height: 100%;
      width: auto;
    }

    /* ── Three-column grid ── */
    .about-grid {
      position: relative;
      z-index: 1;
      display: grid;
      grid-template-columns: 1fr auto 1fr;
      align-items: flex-end;
      gap: 0;
      padding-top: clamp(180px, 28vw, 250px);
      padding-bottom: 0px;
    }

    /* ── Characters ── */
    .about-char {
      display: flex;
      align-items: flex-end;
      overflow: hidden;
    }

    .about-char--left {
      justify-content: flex-start;
    } /* Gul to left wall */
    .about-char--right {
      justify-content: flex-end;
    } /* Vali to right wall */

    .about-char__img {
      display: block;
      width: 100%;
      max-width: clamp(168px, 26vw, 384px);
      height: auto;
      object-fit: contain;
      object-position: bottom center;
      /* Prevent selecting, dragging and saving */
      user-select: none;
      -webkit-user-select: none;
      -webkit-user-drag: none;
      user-drag: none;
    }

    /* ── Center content ── */
    .about-content {
      display: flex;
      flex-direction: column;
      align-items: center;
      text-align: center;
      padding: 0 clamp(20px, 3vw, 48px);
      transform: translateY(-120px);
      gap: clamp(16px, 2.5vw, 44px);
      min-width: clamp(240px, 36vw, 480px);
    }

    .about-badge {
      display: inline-flex;
      align-items: center;
      width: fit-content;
      padding: 6px 16px;
      background: rgba(132, 128, 255, 0.1);
      border: 1px solid rgba(132, 128, 255, 0.25);
      border-radius: 100px;
      font-size: 11px;
      font-weight: 600;
      letter-spacing: 0.1em;
      text-transform: uppercase;
      color: var(--brand-violet);
    }

    .about-title {
      font-family: var(--font-family);
      font-size: clamp(40px, 6vw, 80px);
      font-weight: 700;
      line-height: 1.08;
      letter-spacing: -0.02em;
      color: var(--foreground);
      margin: 0;
    }

    .about-subtitle {
      font-size: clamp(14px, 1.4vw, 17px);
      line-height: 1.65;
      color: var(--foreground-muted);
      max-width: 36ch;
      margin: 0;
    }

    .about-actions {
      display: flex;
      gap: 12px;
      flex-wrap: wrap;
      justify-content: center;
    }

    /* ── Tablet (750px–1024px): keep grid, shrink chars ── */
    @media (min-width: 750px) and (max-width: 1024px) {
      .about-char__img {
        max-width: clamp(160px, 96vw, 320px);
      }
      .about-title {
        font-size: clamp(32px, 5vw, 52px);
      }
    }

    /* ── Mobile (<750px): stack vertically ── */
    @media (max-width: 749px) {
      .about-lamp {
        height: clamp(20px, 84vw, 480px);
        transform: scale(1.3);
      }

      /* Row 1: both characters side-by-side, Row 2: text content */
      .about-grid {
        grid-template-columns: 1fr 1fr;
        grid-template-rows: auto auto;
        padding-bottom: clamp(25px, 0vw, 100px);
      }

      .about-char--left {
        grid-column: 1;
        grid-row: 1;
        justify-content: flex-start;
      }

      .about-char--right {
        grid-column: 2;
        grid-row: 1;
        display: flex; /* restore — hidden before */
        justify-content: flex-end;
      }

      .about-content {
        grid-column: 1 / -1; /* full width */
        grid-row: 2;
        transform: translateY(0px);
        padding: 40px 24px;
        min-width: unset;
      }

      .about-char__img {
        max-width: 100%;
      }

      .about-title {
        font-size: clamp(36px, 9vw, 52px);
      }
    }

    /* ── Dual panels (match contact `.contact-form-side` — minimal, no gradients) ── */
    .about-dual-wrapper {
      width: 100%;
      padding: 12px 12px 0;
      box-sizing: border-box;
    }

    /* Desktop: equal-width columns (50% / 50%) */
    .about-dual-layout {
      display: grid;
      grid-template-columns: 1fr 1fr;
      align-items: stretch;
      margin: 0 auto;
      gap: 12px;
      animation: islandFadeIn 0.6s cubic-bezier(0.2, 0, 0, 1) 0.12s both;
    }

    /* Third section: text column first, image second (same 50/50 track sizes) */
    .about-dual-layout--reverse-desktop .about-dual-panel--story {
      order: 1;
    }

    .about-dual-layout--reverse-desktop .about-dual-panel--media {
      order: 2;
    }

    .about-dual-panel {
      min-width: 0;
      background: var(--surface);
      border: 1px solid var(--border-subtle);
      border-radius: var(--radius-xl);
      padding: 48px;
      box-shadow: var(--shadow-island);
      display: flex;
      flex-direction: column;
      justify-content: center;
    }

    .about-dual-panel--media {
      justify-content: flex-start;
      align-items: stretch;
      align-self: stretch;
      padding: 0;
      overflow: hidden;
      min-height: 0;
    }

    /* flex-basis 0 so this column grows to match the story column; image fills via absolute + cover */
    .about-dual-media-frame {
      position: relative;
      flex: 1 1 0;
      width: 100%;
      min-height: clamp(220px, 38vh, 520px);
      min-width: 0;
      overflow: hidden;
      border-radius: var(--radius-xl);
    }

    .about-dual-media-img {
      position: absolute;
      inset: 0;
      display: block;
      width: 100%;
      height: 100%;
      object-fit: cover;
      object-position: center;
      transform: scale(1);
      transform-origin: center center;
    }

    @media (prefers-reduced-motion: no-preference) {
      .about-dual-media-img {
        transition: transform 0.75s cubic-bezier(0.22, 1, 0.36, 1);
      }

      .about-dual-media-frame:hover .about-dual-media-img,
      .about-dual-media-frame.is-spotlight-active .about-dual-media-img {
        transform: scale(1.045);
      }
    }

    /* Hue blend; spotlight hole grows via --spotlight-r (JS), not opacity */
    .about-dual-media-overlay {
      position: absolute;
      inset: 0;
      z-index: 1;
      pointer-events: none;
      /* Same deep tint in light + dark (matches dark theme switch track) */
      background: #26203c;
      mix-blend-mode: hue;
    }

    .about-dual-media-frame.is-spotlight-active .about-dual-media-overlay {
      -webkit-mask-image: radial-gradient(
        circle var(--spotlight-r, 120px) at var(--spot-x, 50%) var(--spot-y, 50%),
        transparent 0%,
        transparent 40%,
        rgba(0, 0, 0, 0.32) 58%,
        #000 100%
      );
      mask-image: radial-gradient(
        circle var(--spotlight-r, 120px) at var(--spot-x, 50%) var(--spot-y, 50%),
        transparent 0%,
        transparent 40%,
        rgba(0, 0, 0, 0.32) 58%,
        #000 100%
      );
      -webkit-mask-repeat: no-repeat;
      mask-repeat: no-repeat;
    }

    .about-dual-panel--story {
      justify-content: flex-start;
      gap: clamp(20px, 3vw, 28px);
      text-align: left;
      align-items: stretch;
    }

    .about-story-heading {
      font-family: var(--font-family);
      font-size: clamp(22px, 2.75vw, 32px);
      font-weight: 700;
      line-height: 1.18;
      letter-spacing: -0.02em;
      color: var(--foreground);
      margin: 0;
      max-width: 42ch;
    }

    .about-story-heading__accent {
      display: inline-block;
      color: var(--brand-violet);
    }

    :global([data-theme="dark"]) .about-story-heading {
      color: #ffffff;
    }

    .about-story-body {
      display: flex;
      flex-direction: column;
      gap: 1.1em;
    }

    .about-story-body p {
      font-family: var(--font-family);
      font-size: clamp(15px, 1.2vw, 17px);
      line-height: 1.7;
      color: var(--foreground);
      opacity: 0.88;
      margin: 0;
      max-width: 62ch;
    }

    :global([data-theme="dark"]) .about-story-body p {
      color: #e9e8ff;
      opacity: 0.9;
    }

    .about-story-body strong {
      font-weight: 700;
      color: var(--foreground);
      opacity: 1;
    }

    :global([data-theme="dark"]) .about-story-body strong {
      color: #ffffff;
    }

    .about-story-body em {
      font-style: italic;
    }

    .about-story-actions {
      display: flex;
      flex-wrap: wrap;
      gap: 12px;
      align-items: center;
      justify-content: flex-start;
      margin-top: 0.25rem;
    }

    @media (max-width: 900px) {
      .about-dual-layout {
        grid-template-columns: 1fr;
      }

      /* Undo desktop column order so image stays on top on phones */
      .about-dual-layout--reverse-desktop .about-dual-panel--media {
        order: 1;
      }

      .about-dual-layout--reverse-desktop .about-dual-panel--story {
        order: 2;
      }

      .about-dual-panel {
        padding: 24px;
      }

      /* Image panel: no inner padding — photo should bleed to card edges */
      .about-dual-panel.about-dual-panel--media {
        padding: 0;
      }

      .about-dual-panel--media {
        order: 1;
      }

      .about-dual-panel--story {
        order: 2;
        text-align: center;
        align-items: center;
      }

      .about-story-body {
        align-items: center;
      }

      .about-story-body p {
        text-align: center;
      }

      .about-dual-media-frame {
        flex: 0 0 auto;
        width: 100%;
        min-height: 0;
        /* Portrait-friendly frame: image fills edge-to-edge (cover), no side gutters */
        aspect-ratio: 4 / 5;
        max-height: min(82vh, 700px);
        border-radius: var(--radius-xl);
        touch-action: manipulation;
      }

      .about-dual-media-img {
        object-fit: cover;
        object-position: center center;
      }

      .about-story-heading {
        max-width: none;
        margin-left: auto;
        margin-right: auto;
      }

      .about-story-body p {
        max-width: none;
      }

      .about-story-actions {
        flex-wrap: nowrap;
        gap: 12px;
        justify-content: center;
      }

      /* Content-sized like desktop — do not flex-grow: primary buttons use an
         absolutely positioned icon pill; stretching the row breaks padding. */
      :global(.about-story-actions .vp-btn) {
        flex: 0 1 auto;
        min-width: 0;
        width: auto;
      }
    }

    /* Phones only: third section buttons stacked; slightly smaller type on very narrow screens */
    @media (max-width: 600px) {
      .about-dual-panel {
        padding: 20px;
      }

      .about-dual-panel.about-dual-panel--media {
        padding: 0;
      }

      .about-story-actions.about-story-actions--stacked {
        flex-direction: column;
        flex-wrap: nowrap;
        align-items: stretch;
        gap: 12px;
        justify-content: center;
      }

      :global(.about-story-actions--stacked .vp-btn) {
        flex: 0 0 auto;
        width: 100%;
        min-width: 0;
      }

      :global(.about-story-actions .vp-btn) {
        font-size: clamp(13px, 1.35vw, 16px);
      }
    }

    /* ── Reduced motion ── */
    @media (prefers-reduced-motion: reduce) {
      .about-island::before,
      .about-island::after,
      .about-island {
        animation: none !important;
      }

      .about-dual-layout {
        animation: none;
      }
    }
  </style>
</Layout>
